<!DOCTYPE html>
<html lang="en">
<head>
    <title>Edit Task</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #1e1e2e;
            color: #fff;
            text-align: center;
            padding: 20px;
        }

        h1 {
            background: linear-gradient(135deg, #ff7eb3, #ff758c);
            padding: 15px;
            border-radius: 10px;
        }

        form {
            max-width: 400px;
            margin: 20px auto;
            background: #2a2a3a;
            padding: 20px;
            border-radius: 10px;
        }

        label {
            display: block;
            margin-bottom: 10px;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            border-radius: 5px;
            border: none;
            margin-bottom: 10px;
            background: #3a3a4a;
            color: white;
            outline: none;
        }

        input[type="submit"] {
            background: linear-gradient(135deg, #ff7eb3, #ff758c);
            color: white;
            border: none;
            padding: 10px;
            border-radius: 5px;
            width: 100%;
            cursor: pointer;
        }
    </style>
</head>

<body>
    <h1>Edit Task</h1>

    <form action="/todo/edit/<%= todoIdx %>" method="post">
        <p>
            <label for="editTodo">Change to:</label>
            <input type="hidden" name="_method" value="PUT">
            <input type="text" name="editTodo" id="editTodo" autofocus value="<%- todo.text %>" required />
            <input type="submit" value="Save" />
        </p>
    </form>
</body>
</html>

